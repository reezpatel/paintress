{
  "name": "@paintress/plugin-workspace",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/plugin/plugin-workspace/src",
  "projectType": "library",
  "tags": [],
  "// targets": "to see all targets run: nx show project plugin-workspace --web",
  "targets": {
    "watch-deps": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nx watch --projects @paintress/plugin-workspace -- nx build @paintress/plugin-workspace"
      },
      "continuous": true,
      "dependsOn": ["build-deps"]
    }
  }
}
