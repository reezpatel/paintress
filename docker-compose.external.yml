version: "3.8"

services:
  paintress:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:8000"
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - paintress-network

networks:
  paintress-network:
    driver: bridge
